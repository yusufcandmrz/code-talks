import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        margin: 10,
        borderBottomWidth: 1,
        borderColor: "orange",
        color: "orange",
    }
})